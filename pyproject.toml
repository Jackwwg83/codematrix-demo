[project]
name = "changelog-utils"
version = "0.1.0"
description = "Utilities for parsing and formatting CHANGELOG entries"
requires-python = ">=3.10"

[tool.pytest.ini_options]
testpaths = ["."]
python_files = ["test_*.py"]
python_classes = ["Test*"]
python_functions = ["test_*"]