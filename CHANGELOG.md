# Changelog

All notable changes to this project will be documented in this file.

The format is based on [Keep a Changelog](https://keepachangelog.com/en/1.0.0/).

## [Unreleased]

### Added

- Initial project structure with Python package scaffold
- `changelog` utility module with `parse_version` and `format_entry` helpers
- pytest test suite covering `changelog` module functions
- `CHANGELOG.md` tracking project history in Keep a Changelog format
- `pyproject.toml` with project metadata and pytest configuration